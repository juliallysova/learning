function words(arr){
 let short = arr[0];
 let long = arr[0];
 for(let i = 1; i < arr.length; i++){
   if(arr[i].length < short.length){
     short = arr[i];
   }
   if(arr[i].length > long.length){
     long = arr[i];
   }
 }
 return [short, long];
}
