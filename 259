function separatePosAndNeg(arr){
const numPos = []
const numNeg = []
for (let i=0; i <= arr.length; i++){
if (arr[i] < 0){
numNeg.push(arr[i])
} else if (arr[i] > 0) {
numPos.push(arr[i])
}
}
return [numPos,numNeg]
}
