const arr = [3, 7, 4, 9, 2, 10, 1, 17, 6, 23, 5, 13];
let max = arr[0]
let min = arr[0]
for (let i = 1; i < arr.length; i++) {
    if (arr [i] > max) {
        max = arr[i]
    } else if (arr[i] < min) {
      min = arr[i]
    }
}
console.log (max);
console.log(min);
console.log(max-min)
