let str = 'student';
let count = 0
for (let i = 0; i < str.length; i++) {
  if (str[i] === 'a' || str[i] === 'o' || str[i] === 'e' || str[i] === 'u' ||str[i] === 'i') {
    count++
  }
}
console.log(count)
